# Hysteria 2 server — Realms mode
# Rendezvous uses plain HTTP (realm+http://). TLS cert below is for the QUIC proxy only.
#
# If rendezvous runs on the same host, use 127.0.0.1 in the listen URI.
# External clients must use your public IP/domain in their client config instead.
listen: realm+http://VMdg6sjbzmqj35tF2Q0SXSZw5IH09SfP@127.0.0.1:8080/hy2-realm-80ed955ec0c2007b

auth:
  type: password
  password: hdPv7JO81amfBLTbXw147A

tls:
  cert: /etc/hysteria/certs/server.crt
  key: /etc/hysteria/certs/server.key
  sniGuard: disable

masquerade:
  type: proxy
  proxy:
    url: https://www.bing.com
    rewriteHost: true

bandwidth:
  up: 100 mbps
  down: 100 mbps
